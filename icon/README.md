# icon

模块头部 `#!icon` 字段引用的图标资源，放在这里，命名与模块文件同名，例如 `example.png` 对应 `modules/example.sgmodule`。

在模块文件中通过本仓库的 raw 链接引用：

```
#!icon = https://raw.githubusercontent.com/nidadadedaye/Personal/main/icon/example.png
```
